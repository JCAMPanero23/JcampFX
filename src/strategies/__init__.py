"""
JcampFX — Strategy modules package.

Strategies are auto-discovered by BrainCore — any module in this directory
that defines a subclass of BaseStrategy is registered automatically.
(V2.11: new strategy added by dropping a module here, no Brain Core changes needed)
"""
